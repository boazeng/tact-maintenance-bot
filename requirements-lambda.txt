# Runtime deps for the AWS Lambda package (Linux wheels installed by build_lambda.py).
# boto3/botocore are provided by the Lambda runtime, so they are intentionally omitted.
fastapi==0.115.0
pydantic==2.9.2
pydantic-settings==2.6.1
python-dotenv==1.0.1
requests==2.32.3
mangum==0.19.0
